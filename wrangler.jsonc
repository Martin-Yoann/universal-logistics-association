{
    "name": "my-worker",
    "compatibility_date": "2025-12-12",
    "main": "src/index.ts",
    "compatibility_flags": ["nodejs_compat"],
    
    // TypeScript 相关配置
    "tsconfig": "tsconfig.json",  // 如果你的 tsconfig 不在根目录，指定路径
    
    // 构建配置（适用于 tsx/TypeScript）
    "build": {
      "command": "npm run build",  // 如果有构建脚本
      "watch_dir": "src"           // 监视的目录
    }
  }